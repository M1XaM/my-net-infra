# MyNet Infrastructure

1. Pull all three repositories side by side.
2. To run, use `docker compose up --build` from this repository.
3. Access `https://localhost`
4. To stop, use `docker compose down [-v]` (where `-v` would erase the db data also)
