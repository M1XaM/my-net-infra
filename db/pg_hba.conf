# Allow local connections for all users
local   all             all                                     trust

# Allow replication connections from any IP for repl_user
host    replication     repl_user       0.0.0.0/0               md5

# Allow all users to connect from any IP (for testing/dev)
host    all             all             0.0.0.0/0               md5